// eslint-disable-next-line @typescript-eslint/no-var-requires
const { count, expand, expandAll, expandN, toRegExp } = require('./pattern');

module.exports = {
	count,
	expand,
	expandAll,
	expandN,
	toRegExp,
};
